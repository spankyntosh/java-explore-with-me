version: '3.1'
services:
  statistics-server:
    build: ./statistics-service/statistics-server
    container_name: statistics-server
    ports:
      - "9090:9090"
    depends_on:
      - statistics-db
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://statistics-db:5432/statistics
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root

  statistics-db:
    image: postgres:14-alpine
    container_name: statistics-db
    ports:
      - "6541:5432"
    volumes:
      - statistics-db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=statistics
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root

volumes:
  statistics-db: